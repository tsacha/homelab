apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radarr
spec:
  parentRefs:
    - name: gw-media
  hostnames:
    - radarr.{{ .Values.wildcard_domain }}
  rules:
    - backendRefs:
        - name: radarr
          port: 80
