apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin
spec:
  parentRefs:
    - name: gw-media
  hostnames:
    - jellyfin.{{ .Values.wildcard_domain }}
  rules:
    - backendRefs:
        - name: jellyfin
          port: 80
