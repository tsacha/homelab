apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr
spec:
  parentRefs:
    - name: gw-media
      sectionName: https
  hostnames:
    - sonarr.{{ .Values.wildcard_domain }}
  rules:
    - backendRefs:
        - name: sonarr
          port: 80
