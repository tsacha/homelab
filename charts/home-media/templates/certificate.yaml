apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard
spec:
  secretName: wildcard-cert
  dnsNames:
    - "{{ .Values.wildcard_domain }}"
    - "*.{{ .Values.wildcard_domain }}"
  issuerRef:
    name: le-home-prod
    kind: ClusterIssuer
    group: cert-manager.io
