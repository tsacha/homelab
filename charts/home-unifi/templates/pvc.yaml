apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unifi-app-pvc
  namespace: unifi
  annotations:
    k8up.io/backup: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
