apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: unifi-app
  namespace: unifi
spec:
  parentRefs:
    - name: gw-unifi
      sectionName: https
  hostnames:
    - unifi.{{ .Values.wildcard_domain }}
  rules:
    - backendRefs:
        - name: unifi-http
          port: 80
