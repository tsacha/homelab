apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vaultwarden
spec:
  secretName: vaultwarden-cert
  dnsNames:
    - "{{ .Values.pass.vaultwarden_vhost }}"
  issuerRef:
    name: le-prod
    kind: ClusterIssuer
    group: cert-manager.io
