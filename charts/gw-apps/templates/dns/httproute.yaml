apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http2136
spec:
  parentRefs:
    - name: gw-apps
  hostnames:
    - "{{ .Values.dns.http2136_vhost }}"
  rules:
    - backendRefs:
        - name: http2136
          port: 2136
