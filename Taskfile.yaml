# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"
tasks:
  default: go-task --list
includes:
  templates: ./tasks/templates.yaml
  boot: ./tasks/boot.yaml
  gw: ./tasks/gw.yaml
  talos: ./tasks/talos.yaml
  renovate: ./tasks/renovate.yaml
