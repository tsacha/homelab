apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: http2136
spec:
  secretName: http2136-cert
  dnsNames:
    - "{{ .Values.dns.http2136_vhost }}"
  issuerRef:
    name: le-prod
    kind: ClusterIssuer
    group: cert-manager.io
