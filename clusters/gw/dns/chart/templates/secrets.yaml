apiVersion: v1
kind: Secret
metadata:
  name: dns
type: Opaque
stringData:
  rndc.key: |
    key "rndc-key" {
            algorithm hmac-sha256;
            secret "{{ .Values.dns.rndc_key }}";
    };
