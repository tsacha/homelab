apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vaultwarden
spec:
  secretName: vaultwarden-cert
  dnsNames:
    - "${VAULTWARDEN_VHOST}"
  issuerRef:
    name: le-prod
    kind: ClusterIssuer
    group: cert-manager.io
