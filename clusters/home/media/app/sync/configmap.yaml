apiVersion: v1
kind: ConfigMap
metadata:
  name: rclone-config
data:
  rclone.conf: |
    [seedbox]
    type = sftp
    host = ${SEEDBOX_HOST}
    user = ${SEEDBOX_USER}
    key_file = /root/.ssh/id_ed25519
    shell_type = unix
    md5sum_command = md5sum
    sha1sum_command = sha1sum
